{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run --watch index.ts",
    "tunnel": "npx localtunnel --port 3000 --subdomain podium-proof"
  },
  "devDependencies": {
    "@types/bun": "^1.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.22.0",
    "dotenv": "^16.4.5",
    "openai": "^4.47.3"
  }
}
